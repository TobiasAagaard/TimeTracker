using Microsoft.Extensions.Configuration;

namespace TimeTracker.Cli.Configuration;

public sealed class KeyParser
{
    public static class KeyParser {
        public static KeyBindings Parse(KeyBindingsOptions)
    }
}